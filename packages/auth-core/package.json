{
  "name": "@workspace/auth-core",
  "version": "0.0.1",
  "description": "Core package for authentication and authorization contracts",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "dev": "tsc --watch",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@workspace/eslint-config": "workspace:*",
    "@workspace/typescript-config": "workspace:*",
    "rimraf": "^5.0.5",
    "typescript": "^5.3.3"
  }
}
